const BASE_URL = 'api/verifications';

export const VERIFICATION_ENDPOINTS = {
    VERIFICATION: `${BASE_URL}/`,
    UPDATE_VERIFICATION: `${BASE_URL}/:id/status`,
    PERSONAL_VERIFICATIONS: `${BASE_URL}/personal-verifications`
};
